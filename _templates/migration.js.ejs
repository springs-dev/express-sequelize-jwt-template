module.exports = {
  up: (queryInterface, Sequelize) => {
    return queryInterface.createTable('<%= pluralModelName.camelCase %>', {
      id: {
        type: Sequelize.UUID,
        defaultValue: Sequelize.literal('uuid_generate_v1()'),
        allowNull: false,
        primaryKey: true,
      },<% for (attributeName in modelAttibutes) { %>
      <%= attributeName %>: {
        type: Sequelize.<%- modelAttibutes[attributeName].migration %>,
      },<% } %>
      createdAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },
      updatedAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },
    });
  },
  down: (queryInterface) => {
    return queryInterface.dropTable('<%= pluralModelName.camelCase %>');
  },
};
